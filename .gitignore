target/
*.class
.idea/
*.iml
.DS_Store
spark-warehouse/
node_modules/
frontend/dist/

# Figma Make platform-provisioned config (not present outside the Figma Make sandbox;
# vite.config.ts statically imports frontend/.figma/make/site.json, so a local stub
# is needed to run vite/vitest/pnpm build outside that sandbox)
frontend/.figma/

parser/src/main/resources/DWH_CONTROL
parser/src/main/scala/io/pure360/ipc/xmltojson/doc
.worktrees/

/config.json
/.env

# User-local scratch that has carried REAL infrastructure identifiers (project ids,
# job ids, cluster names) pasted in from live consoles. Untracked is not enough —
# a single `git add -A` would commit them. Ignored so it cannot be staged by accident.
/first_prompt.md
